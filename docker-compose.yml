services:
  api:
    build: .
    container_name: vn_news_summarizer
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./models:/app/models
      - ./secrets:/app/secrets:ro
